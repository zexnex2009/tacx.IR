[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "runtacx"
version = "0.1.0"
description = "PyQt runner for Tacx.IR programs"
requires-python = ">=3.10"
dependencies = [
  "PyQt5>=5.15",
]

[project.scripts]
runtacx = "RunTacx.main:main"
